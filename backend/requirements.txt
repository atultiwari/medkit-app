# Grand Rounds backend — FastAPI server (Managed Agents proxy + LiveKit token mint).
# The real-time voice worker (voice_agent.py) lives in its own venv with its
# own requirements file (voice_agent_requirements.txt) so its deps don't
# tangle with this one.

fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
anthropic>=0.88.0
livekit-api~=0.8
httpx==0.28.1
slowapi==0.1.9

# Offline-first provider deps (Ollama / Gemini both use httpx — no SDK needed).
# faster-whisper is optional for in-process STT; the sidecar container
# installs it independently.
